# Gradle properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Project properties
version=1.2.0
group=com.xsyphon

# Skip native build during development (set to true to skip)
skipNativeBuild=false

# Publishing credentials (override with environment variables or local settings)
# ossrhUsername=your_username
# ossrhPassword=your_password
# signingKey=your_gpg_key
# signingPassword=your_gpg_password
